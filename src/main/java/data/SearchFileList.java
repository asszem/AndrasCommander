package data;

public class SearchFileList {
    //KeyBindingParser triggers this when a search request is parsed
    // Gets the search term
    //parses through the file list
    //Returns a list with the matched file indexes
    //
}
